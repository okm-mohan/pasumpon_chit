(() => {const modal=document.getElementById("eventModal"),cards=[...document.querySelectorAll(".event-card")],close=document.querySelector(".event-close"),image=document.getElementById("eventImage"),title=document.getElementById("eventTitle"),date=document.getElementById("eventDate"),place=document.getElementById("eventPlace"),description=document.getElementById("eventDescription"),form=document.getElementById("commentForm"),list=document.getElementById("commentList");const open=card=>{image.src=card.dataset.image;title.textContent=card.dataset.title;date.textContent=card.dataset.date;place.innerHTML='<i class="fa-solid fa-location-dot"></i> '+card.dataset.place;description.textContent=card.dataset.description;list.innerHTML='<p class="empty-comment">இந்த நிகழ்வைப் பற்றி முதல் கருத்தை பதிவு செய்யுங்கள்.</p>';modal.classList.add("show");modal.setAttribute("aria-hidden","false")};cards.forEach(card=>card.addEventListener("click",()=>open(card)));const closeModal=()=>modal.classList.remove("show");close.addEventListener("click",closeModal);modal.addEventListener("click",event=>{if(event.target===modal)closeModal()});document.addEventListener("keydown",event=>{if(event.key==="Escape")closeModal()});form.addEventListener("submit",event=>{event.preventDefault();const name=document.getElementById("commentName"),text=document.getElementById("commentText");if(!name.value.trim()||!text.value.trim())return;const empty=list.querySelector(".empty-comment");if(empty)empty.remove();const row=document.createElement("div");row.className="comment";row.innerHTML='<b></b><span></span>';row.querySelector("b").textContent=name.value.trim();row.querySelector("span").textContent=text.value.trim();list.prepend(row);text.value=""})})();