const caMoney=v=>`₹${Number(v||0).toLocaleString('en-IN',{maximumFractionDigits:0})}`;document.addEventListener('DOMContentLoaded',async()=>{try{const r=await fetch('/api/accounts/company-dashboard');const d=await r.json();for(const[id,v]of Object.entries({caBalance:d.balance,caOpening:0,caCredits:d.credits,caDebits:d.debits,caToday:d.today_net,caMonth:d.month_net,caCash:d.modes.CASH,caUpi:d.modes.UPI,caBank:d.modes.BANK})){document.getElementById(id).textContent=caMoney(v)}document.getElementById('caRecent').innerHTML=d.recent.length?d.recent.map(x=>`<div class="ca-row"><span class="ca-row-icon ${x.transaction_type==='CREDIT'?'in':'out'}"><i class="bi bi-${x.transaction_type==='CREDIT'?'arrow-down-left':'arrow-up-right'}"></i></span><div><strong>${x.category||'Ledger entry'}</strong><small>${x.transaction_date} · ${x.payment_mode||'Cash'}</small></div><b class="${x.transaction_type==='CREDIT'?'in':'out'}">${x.transaction_type==='CREDIT'?'+':'−'} ${caMoney(x.amount)}</b></div>`).join(''):'<p>No ledger transactions yet.</p>'}catch(_){document.getElementById('caRecent').textContent='Unable to load company account balance.'}});
