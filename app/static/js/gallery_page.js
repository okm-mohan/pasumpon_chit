(() => { const stream=document.getElementById("galleryStream"), modal=document.getElementById("galleryModal"), img=document.getElementById("modalImage"), caption=document.getElementById("modalCaption"), download=document.getElementById("modalDownload"); if(!stream||!modal)return; const items=[...document.querySelectorAll(".gallery-open")]; let index=0; const show=(next)=>{index=(next+items.length)%items.length;const item=items[index];img.src=item.dataset.full;img.alt=item.dataset.caption;caption.textContent=item.dataset.caption;download.href=item.dataset.full;modal.classList.add("show");modal.setAttribute("aria-hidden","false")};document.querySelectorAll(".size-btn").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".size-btn").forEach(x=>x.classList.remove("active"));btn.classList.add("active");stream.className="gallery-stream size-"+btn.dataset.size;}));items.forEach((item,i)=>{ item.addEventListener("click",()=>show(i)); const photo=item.closest("figure")?.querySelector("img"); if(photo){photo.addEventListener("click",()=>show(i));} });document.querySelector(".modal-close").addEventListener("click",()=>modal.classList.remove("show"));document.querySelector(".modal-nav.prev").addEventListener("click",()=>show(index-1));document.querySelector(".modal-nav.next").addEventListener("click",()=>show(index+1));modal.addEventListener("click",e=>{if(e.target===modal)modal.classList.remove("show")});document.addEventListener("keydown",e=>{if(!modal.classList.contains("show"))return;if(e.key==="Escape")modal.classList.remove("show");if(e.key==="ArrowLeft")show(index-1);if(e.key==="ArrowRight")show(index+1)});})();