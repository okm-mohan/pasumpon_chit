(() => {const stream=document.getElementById("galleryStream"),modal=document.getElementById("galleryModal"),image=document.getElementById("modalImage"),caption=document.getElementById("modalCaption"),download=document.getElementById("modalDownload");if(!stream||!modal)return;const items=[...document.querySelectorAll(".video-open")];let current=0;const show=i=>{current=(i+items.length)%items.length;const item=items[current];image.src=item.dataset.full;image.alt=item.dataset.caption;caption.textContent=item.dataset.caption;download.href=item.dataset.full;modal.classList.add("show");modal.setAttribute("aria-hidden","false")};document.querySelectorAll(".size-btn").forEach(button=>button.addEventListener("click",()=>{document.querySelectorAll(".size-btn").forEach(x=>x.classList.remove("active"));button.classList.add("active");stream.className="gallery-stream size-"+button.dataset.size}));items.forEach((item,i)=>{item.addEventListener("click",()=>show(i));item.closest("figure").querySelector("img").addEventListener("click",()=>show(i))});document.querySelector(".modal-close").addEventListener("click",()=>modal.classList.remove("show"));document.querySelector(".modal-nav.prev").addEventListener("click",()=>show(current-1));document.querySelector(".modal-nav.next").addEventListener("click",()=>show(current+1));modal.addEventListener("click",event=>{if(event.target===modal)modal.classList.remove("show")});document.addEventListener("keydown",event=>{if(!modal.classList.contains("show"))return;if(event.key==="Escape")modal.classList.remove("show");if(event.key==="ArrowLeft")show(current-1);if(event.key==="ArrowRight")show(current+1)});})();